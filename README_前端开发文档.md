# 会计事务所资料收集系统 - 前端开发文档

## 1. 文档用途

本文把[系统架构设计](./README_系统架构设计.md)拆成可执行的前端开发阶段，并与[后端开发文档](./README_后端开发文档.md)对应。Mock 可以帮助阶段内并行开发，但每个阶段的最终验收必须连接真实后端 API。

前端仓库：`acc-system-frontend`

技术基线：Vue 3、TypeScript、Vite、shadcn-vue、Pinia、Vue Router、Vue I18n、Axios、`axios-case-converter`、pnpm。

## 2. 开发与验收规则

### 2.1 通用完成标准

每个阶段同时满足以下条件才算完成：

1. `pnpm lint` 和 `pnpm build` 通过，不能用关闭规则或 TypeScript 检查规避真实错误；
2. 本阶段新增的状态、字段转换和权限显示逻辑有最小自动测试；
3. 最终验收连接对应阶段的真实后端，不以 Mock 响应代替；
4. 页面覆盖 loading、空数据、成功、业务错误、网络错误和无权限状态；
5. 表单有 label、键盘可操作、清晰焦点和可读错误提示，状态不能只靠颜色表达；
6. 所有 HTTP 请求通过统一 Axios client，不在组件内直接使用 `fetch` 或新建 Axios 实例；
7. 验收记录保存测试日期、前后端提交 SHA、镜像标签、浏览器和失败截图。
8. 用户可见文案不直接写在组件中；英文消息必须完整，缺失翻译回退到英文。

### 2.2 状态与接口规则

- Pinia 只保存认证状态和少量跨页面 UI 偏好；
- 列表筛选、分页和排序写入 URL query；
- 页面业务数据从 API 获取，不建立第二套客户端数据库；
- access JWT 只保存在请求模块的内存变量，不写 localStorage/sessionStorage；Pinia 保存用户资料与认证状态；
- refresh JWT 只由 HttpOnly Cookie 携带；
- API snake_case 与前端 camelCase 只在 Axios 边界转换；
- portal 与会计端使用不同响应类型，前端不依赖后端内部字段；
- 按钮是否显示只改善体验，权限与状态最终由后端判断。
- Vue I18n 默认 locale 和 fallback locale 都是 `en`，简体中文使用 `zh-CN`；
- 仅在用户主动切换后保存 locale 偏好；新浏览器和无偏好用户始终先显示英文；
- API 返回稳定的错误码和枚举值，前端负责翻译展示文案，不翻译协议字段和值。

### 2.3 建议命令

```bash
pnpm install --frozen-lockfile
pnpm lint
pnpm build
pnpm test # F2 开始
docker build -t acc-system-frontend:local .
```

Playwright 只在最终关键流程需要浏览器自动化时加入；组件和纯逻辑优先使用 Vitest，避免首版维护大规模脆弱的端到端套件。

## 3. 阶段总览

| 阶段 | 前端交付结果 | 依赖后端 |
| --- | --- | --- |
| F1 部署基线 | 静态镜像、SPA fallback、ECR 发布和服务器访问可用 | B1 |
| F2 应用骨架与登录 | Router、Pinia、Vue I18n、Axios、JWT 刷新和两套布局 | B2 |
| F3 账户与客户管理 | 员工、联系人、客户和会计分配页面 | B2 |
| F4 会计端收集请求 | Dashboard、列表、创建、详情、发布、复制和事件 | B3 |
| F5 客户门户与上传 | 要求查看、隔离上传状态、草稿和首次提交 | B4 |
| F6 审核审批与上线 | 人工审核工作台、多轮补交、批准和全流程回归 | B5 |
| F7 AI 分类与审核 | 每请求 AI 策略、上传快速分类、提交后完整审核与人工覆盖 | B6、A1-A3 |

F1 与 B1 是同一个部署阶段的两部分，可以并行开发；F1 可先单独验证静态镜像，联合验收时必须使用 B1 的真实 Compose、Nginx 和健康接口。

## 4. F1：部署基线

### 4.1 阶段目标

先确认当前 Vue 工程可以稳定构建成镜像，经 ECR 发布并由服务器上的总入口 Nginx 访问。本阶段不实现登录和业务页面。

### 4.2 交付内容

- 保留多阶段 Dockerfile：Node/pnpm 构建，静态 Web Server 运行；
- 固定 Node 与 pnpm 版本，使用 `pnpm-lock.yaml` 和 `--frozen-lockfile`；
- 静态容器监听内部端口，不直接映射服务器公网端口；
- 静态服务支持 SPA fallback，刷新 `/login` 等前端路由不会返回 404；
- `.dockerignore` 排除 node_modules、dist、Git 和本地环境文件；
- GitHub Actions 在 PR 执行构建，在 main/tag 使用 AWS OIDC 推送 ECR；
- 镜像使用 Git SHA 不可变标签；
- 前端运行配置首版只使用同域 `/api/v1`，不把生产后端域名编译进镜像；
- 页面提供最小的构建版本信息，便于确认当前部署 SHA。

本阶段不增加组件库封装、状态管理、测试框架或业务路由；这些从 F2 开始按实际需要加入。

### 4.3 验收前置

- B1 的 Compose 与总入口 Nginx 已可运行；
- frontend ECR repository 和 GitHub OIDC 变量已配置；
- 服务器 `.env` 已固定 `FRONTEND_IMAGE` 的 SHA 标签。

### 4.4 可验收场景

| ID | 场景与操作 | 预期结果 |
| --- | --- | --- |
| F1-A1 | 执行 `pnpm install --frozen-lockfile && pnpm lint && pnpm build` | ESLint、TypeScript 和构建通过并生成 dist |
| F1-A2 | 构建并启动前端镜像，直接请求容器根路径 | 返回 Vue 页面，容器不需要 Node 开发服务器 |
| F1-A3 | 通过总入口 Nginx 请求 `/` 和 `/login`，并刷新 `/login` | 两个地址都返回 SPA，刷新不出现 Nginx 404 |
| F1-A4 | 请求 `/api/v1/health/live` | 请求由 Nginx 转发给 backend，不被前端 fallback 截获 |
| F1-A5 | 推送 main 后查看 GitHub Actions 与 ECR | 构建通过，ECR 出现对应 Git SHA 标签，无长期 AWS key |
| F1-A6 | 服务器固定新 SHA 并执行 Compose pull/up | 页面展示的构建 SHA 与部署值一致；切回旧 SHA 可以恢复上一镜像 |
| F1-A7 | 检查最终镜像 | 不包含源码仓库、node_modules、`.env` 或构建凭据；仅暴露内部静态服务端口 |

### 4.5 阶段完成标志

任一前端提交都能生成可追踪的不可变镜像，并通过后端仓库维护的 Compose/Nginx 在服务器上访问。

## 5. F2：应用骨架、Axios 与登录

### 5.1 阶段目标

建立后续页面共用的路由、布局、认证和请求边界，一次解决 JWT 刷新、字段命名转换与错误处理。

### 5.2 交付内容

- 安装并配置 Vue Router、Pinia、Vue I18n、Axios、`axios-case-converter`；
- 建立 `en` 与 `zh-CN` 消息文件；默认和 fallback 均为 `en`，页面文案、校验提示、状态与错误码统一使用翻译 key；
- 语言切换集中在账户设置中，不占用登录页、工作台或 404 的页头；持久化 locale，并同步更新根元素的 `lang` 属性；
- 账户设置提供常青绿（默认）、石板灰、蓝色配色，以及浅色、深色、跟随系统（默认）三种显示模式；偏好保存到当前浏览器；
- 外观在首屏绘制前初始化；跟随系统模式实时响应系统外观变化，登录页、工作台、Select 菜单和 404 共用主题色；
- `src/api/client.ts` 是唯一请求入口，业务 Axios 实例和无 refresh 重试的会话实例共用 `baseURL=/api/v1`、`withCredentials=true`；
- 请求 data/params 转 snake_case，响应 data 转 camelCase，headers、FormData、File 和 Blob 不被破坏；
- auth store 将 access JWT 同步到请求模块的内存变量，request interceptor 从内存读取；不写入浏览器持久化存储；
- response interceptor 使用共享 refresh Promise，并发 401 只刷新一次，每个原请求最多重试一次；
- 应用启动时调用 refresh 恢复会话，失败后进入未登录状态；
- `/login`、会计端 layout、客户端 portal layout 和 404 页面；
- 路由 meta 区分公开页、事务所端和客户端；
- 登录、退出、修改密码和当前用户页面；
- 统一处理 401、403、404、409、422、429 和 5xx；`request_id` 在错误详情中可复制；
- Vitest 覆盖字段转换、refresh 并发和重试终止条件。

### 5.3 可验收场景

| ID | 场景与操作 | 预期结果 |
| --- | --- | --- |
| F2-A1 | 使用有效账号登录 | 跳转到角色对应首页；access JWT 仅存在内存，浏览器存储中不存在 token |
| F2-A2 | 刷新浏览器或重新打开页面 | 依靠 HttpOnly Cookie 恢复会话，用户无需重复输入密码 |
| F2-A3 | 人为令 access JWT 过期后同时发起多个请求 | 浏览器只发送一个 refresh；原请求各重试一次并成功 |
| F2-A4 | refresh 也过期或被撤销 | 清空 auth store，跳转登录页，不出现无限 401/refresh 循环 |
| F2-A5 | ACCOUNTANT 和 CLIENT_SUBMITTER 分别访问对方路由 | 前端给出无权限页面或正确跳转；直接调用 API 仍由后端拒绝 |
| F2-A6 | 提交带嵌套对象、数组和 query 的请求 | API 收到 snake_case，组件获得 camelCase；Authorization/Idempotency-Key 未被改名 |
| F2-A7 | 上传 File 和下载 Blob | 转换中间件不破坏二进制对象，错误响应仍能显示后端 code 与 request_id |
| F2-A8 | 使用键盘完成登录并触发字段错误 | label、焦点顺序和错误提示可用，错误不只依赖红色边框 |
| F2-A9 | 使用无 locale 偏好的新浏览器首次打开系统 | 页面显示英文，根元素为 `lang="en"` |
| F2-A10 | 切换到简体中文后刷新，再模拟一个缺失的中文翻译 key | 刷新后保持 `zh-CN`；缺失项显示英文 fallback，不显示原始 key |
| F2-A11 | 在账户设置分别选择三种配色及浅色/深色，访问工作空间和 404 后刷新 | 配色和模式保持，页面、输入框和 Select 菜单均正确适配，无大块固定白色背景 |
| F2-A12 | 选择跟随系统，再切换系统外观；随后固定选择浅色或深色并再次切换系统外观 | 跟随系统时立即更新；固定模式不被系统变化覆盖 |
| F2-A13 | 保存深色偏好后重新打开页面 | 首屏按深色显示，不先闪现浅色；无偏好时使用常青绿和跟随系统 |

### 5.4 阶段完成标志

后续页面只调用领域 API 模块，不再处理 token、refresh、字段转换、通用错误或自行维护界面文案。

### 5.5 本轮验收记录（2026-09-17）

- 用户已完成本地界面验收；本轮保持认可的布局、Select、404、账户内语言与外观设置。
- 交付提交：前端 `3801ff0c93a5a5922426fe666b221408c6cc2dbc`，配套后端 `9221ffb1af062cc5c977c58ad460832a347b5e0a`。
- 自动检查：`pnpm lint`、`pnpm build`、Vitest **17 passed**；覆盖真实 Axios 并发 401、晚到 401、失败终止、字段/二进制转换、角色路由、立即退出失效会话、语言和外观偏好。
- 本地验收入口：`http://localhost`；前端镜像 `acc-system-frontend:local` 使用以上提交 SHA 作为构建版本。本轮不更新 Lightsail。
- 当前可操作的是登录、工作空间、账户与改密。员工、客户、邀请接受页面继续在 F3 完成，不视为本轮已交付；Email/飞书实际发送仍待 B6。
- 本轮新增回归由自动测试执行；不重置用户已修改的本地管理员密码。

## 6. F3：账户与客户管理

### 6.1 阶段目标

让事务所管理员维护员工、客户、联系人和负责人，让客户管理员维护本客户联系人。

### 6.2 交付内容

- `/staff/admin/users`：员工列表、邀请、启用/禁用；
- `/staff/clients`：客户列表、搜索、分页和状态；
- `/staff/clients/:id`：基本信息、业务特征、银行账户、联系人和会计分配；
- `/client/contacts`：客户管理员维护本客户联系人；
- `/invitations/accept`：受邀人设置密码并接受邀请；
- CLIENT_ADMIN 可见的联系人管理入口；
- 邀请状态、过期、撤销和重新邀请反馈；
- 固定角色选择，不实现自定义 RBAC 编辑器；
- 表单提交期间禁止重复点击，409/422 显示可行动说明；
- 无权限动作不显示，但仍正确处理后端返回的权限错误。

### 6.3 可验收场景

| ID | 场景与操作 | 预期结果 |
| --- | --- | --- |
| F3-A1 | FIRM_ADMIN 创建客户并邀请会计和客户管理员 | 列表和详情立即反映状态，受邀者能完成 B2 邀请流程 |
| F3-A2 | 为客户分配/取消分配会计 | 页面显示最新负责人；受影响会计刷新后访问范围同步变化 |
| F3-A3 | CLIENT_ADMIN 邀请本客户联系人 | 操作成功且只能选择客户端角色 |
| F3-A4 | ACCOUNTANT 或 CLIENT_SUBMITTER 尝试打开管理员页面 | 导航不显示入口，直接输入 URL 得到无权限结果 |
| F3-A5 | 快速重复点击邀请或保存 | 只产生一次有效请求，不生成重复邀请或重复客户 |
| F3-A6 | 列表翻页后进入详情再返回 | URL 中的搜索、分页和筛选条件保留 |
| F3-A7 | 后端返回 409/422/429 | 页面保留用户输入并显示明确处理方式，不只弹出“请求失败” |

### 6.4 阶段完成标志

管理员无需数据库或 SQLAdmin 即可完成首版账户、客户和负责人维护。

### 6.5 本地验收候选（2026-09-17，待用户验收）

- 入口：`http://localhost`，使用现有本地管理员账户；不要重置其密码。本轮未更新 Lightsail，也未提交代码。
- 可用 `F3 Acceptance Demo` 样例客户检查客户详情、银行账户和团队页；员工与客户列表从顶部导航进入。邀请只生成一次性链接，需手动分享，不发送 Email/飞书。
- 按 F3-A1～A7 验收；受邀角色和客户端联系人需使用各自账号测试，不能用事务所管理员账号代替。
- 自动检查：前端 `pnpm lint`、`pnpm build`、Vitest **36 passed**；后端 `uv run pytest -q` **25 passed**、`uv run alembic check` 无模型漂移。本地 Compose 的前后端、Nginx、PostgreSQL、Redis 与 worker 均已启动，`/api/v1/health/ready` 返回 PostgreSQL/Redis 正常。

## 7. F4：会计端收集请求

### 7.1 阶段目标

让会计人员从待办列表创建和发布月度资料收集请求，并能稳定跟踪进度和历史事件。

### 7.2 交付内容

- `/app/dashboard`：待我审核、等待客户、即将到期、已逾期；
- `/app/collections`：服务端分页，按客户、期间、状态、负责人和截止日筛选；
- 筛选、分页和排序写入 URL query；
- 桌面端列表右侧详情面板，窄屏直接进入完整详情；
- `/app/collections/new`：使用 shadcn-vue `Stepper` 分为请求信息、资料清单和确认三步，最终一次提交；
- 期间和截止日使用 shadcn-vue `Calendar + Popover`，列表截止日筛选使用 `Range Calendar`；
- 请求详情显示状态、版本、负责人、要求和提交轮次，活动记录使用纵向 `Stepper`；
- 业务动作按钮：保存草稿、发布、复制上月、取消；不提供任意状态下拉框；
- 写操作生成并复用一次 `Idempotency-Key`，网络重试不生成新 key；
- version 冲突显示最新数据和刷新入口，不静默覆盖。

### 7.3 可验收场景

| ID | 场景与操作 | 预期结果 |
| --- | --- | --- |
| F4-A1 | 会计创建包含多个 requirement 的草稿 | 保存成功，详情显示完整要求和 DRAFT 状态 |
| F4-A2 | 必填项缺失或后端校验失败 | 定位到具体字段，用户输入保留，不产生半张请求 |
| F4-A3 | 发布请求后刷新页面 | 状态为 OPEN，显示发布事件；已发布要求不再出现删除入口 |
| F4-A4 | 网络超时后重试发布 | 前端复用同一 Idempotency-Key，最终只显示一次发布事件 |
| F4-A5 | 两个浏览器编辑同一草稿 | 后提交者收到版本冲突提示，可查看最新数据后重新操作 |
| F4-A6 | 复制上月请求到新期间 | 表单预填 requirements，但不展示为已上传或已审核 |
| F4-A7 | 组合筛选列表、打开详情再返回 | URL 和列表位置保持，数据只包含当前用户有权访问的客户 |
| F4-A8 | 在窄屏和桌面分别操作列表 | 桌面使用详情面板，窄屏进入完整页；核心操作均可完成 |

### 7.4 阶段完成标志

会计可以从 Dashboard 找到工作、创建并发布真实请求，刷新、重试和多人操作不会造成不明确状态。

### 7.5 本地验收候选（2026-09-17，待用户验收）

- 入口：`http://localhost:5173`，使用现有本地会计或管理员账户；前端由 Vite 提供，API 经本地 Nginx 转发。本轮未提交代码、未更新 Lightsail。
- 已建立 `F3 Acceptance Demo / 2026-10` 样例请求，包含三项资料要求并已发布，可直接检查 Dashboard、列表右侧详情和事件时间线。
- 按 F4-A1～A8 验收；“复制到下个月”只预填新建表单，必须再次保存才会产生新请求。
- 自动检查：`pnpm lint`、`pnpm build`、Vitest **38 passed**；390px 窄屏与桌面 split view 已执行浏览器检查。

## 8. F5：客户门户、文件上传与首次提交

### 8.1 阶段目标

让客户清楚知道缺什么、已经上传什么，并能安全完成首次提交。退回原因和补交轮次在 F6 与审核流程一起验收。

### 8.2 交付内容

- `/portal/collections` 与 `/portal/collections/:id`；
- 顶部显示期间、截止日、负责人、整体进度和当前状态；
- requirement 可展开，显示说明、已上传文件、处理状态和公开审核意见；
- 上传区绑定 requirement，支持文件选择和拖放，并提供“其他支持文件”；
- 智能上传可一次接收多个文件；先二次确认已选文件，再进入 AI 加载态；结果按 requirement 和“无效文件”分组展示，支持拖放调整，最终确认后才上传；
- Axios `onUploadProgress` 显示进度，上传完成后轮询 `QUARANTINED → AVAILABLE/FAILED`；
- 草稿文件可逻辑排除；进入补交后按原顺序展示历史有效文件并在末尾追加新文件，客户可从本轮有效清单移除旧文件，但历史提交记录保持不变；
- 页面底部主动作“提交审核”；F6 接入补交状态后复用为“再次提交”；
- 每次提交或再次提交可填写一条可选备注，备注只属于当前提交轮次；
- 提交前列出未上传或仍需处理的必填项，但最终判断以后端结果为准；
- 网络中断支持明确重试，不把同一文件显示成多个成功上传；
- portal 类型中不存在 internalNote 和 AI 原始输出字段。

### 8.3 可验收场景

| ID | 场景与操作 | 预期结果 |
| --- | --- | --- |
| F5-A1 | 客户打开一个 OPEN 请求 | 看见期间、截止日、所有要求和明确的待办状态 |
| F5-A2 | 上传正常文件 | 显示上传进度，随后展示扫描中，最终变为可用 |
| F5-A3 | 上传超限、类型不符或扫描失败文件 | 显示明确失败原因和重新上传入口，文件不能被预览/下载 |
| F5-A4 | 文件尚在 QUARANTINED 时尝试提交 | 页面提示仍在处理；绕过前端提交时以后端规则为准 |
| F5-A5 | 上传多个文件、排除一个、填写可选备注并提交 | 确认页只列出有效文件；备注随本轮保存；提交后当前轮次和其中的文件变为只读，不再显示移除入口 |
| F5-A6 | 客户尝试访问另一个客户的 URL 或 document id | 不显示任何目标数据，错误页不泄漏资源是否存在 |
| F5-A7 | 检查页面响应和开发者工具 | 看不到 internalNote、AI 原始输出、文件磁盘路径或其他事务所字段 |
| F5-A8 | 使用键盘选择文件、查看错误并提交 | 核心操作无需鼠标即可完成，状态同时有文字/图标而非仅颜色 |
| F5-A9 | 将对账单、收据、其他支持文件和无效文件一次拖入智能上传区 | 先显示已选文件确认页；确认后展示 AI 处理动画，随后按类别展示结果；可拖动文件调整分类，最终确认后有效文件才上传；中途取消时必须再次确认，确认取消后清单不变 |

### 8.4 阶段完成标志

客户可以在门户中完成首次资料提交；F6 再完成退回说明和多轮补交闭环。

## 9. F6：会计审核、审批与上线验收

### 9.1 阶段目标

完成会计审核工作台和最终审批，并用少量稳定的浏览器测试覆盖关键业务闭环。

### 9.2 交付内容

- `/app/collections/:id/review` 三块区域：要求列表、文件预览/提取字段、审核动作；
- 页面级提交轮次切换统一过滤资料项文件、客户备注和审核结论；默认最新轮，历史轮只读；
- 活动时间线统一放在请求详情页；审核页不重复维护第二份时间线；
- 当前轮文件随审核结论自动记录，无需会计逐份选择；
- 单项满足、要求补交和豁免；公开客户说明与内部备注分开输入和展示；
- 整单要求补交、批准和管理员撤回批准；`READY_FOR_BOOKKEEPING` 在资料收集前端中作为终态；
- 按当前状态和角色映射展示可用动作，但不在前端复制完整状态机，后端仍作最终判断；
- AI 建议使用独立标识，必须点击“采纳并填写审核决定”才进入人工表单；
- version 冲突时保留未提交文字并提示加载最新状态；
- 对破坏性动作、豁免、批准和撤回使用确认弹窗；普通单项审核不重复确认；
- 关键路径 Playwright：登录、创建发布、客户上传提交、退回补交、审核批准；
- 桌面与常用窄屏宽度的可用性、焦点、错误和空状态检查。

### 9.3 可验收场景

| ID | 场景与操作 | 预期结果 |
| --- | --- | --- |
| F6-A1 | 会计审核客户提交的错主体文件 | 文件保留，requirement 显示 `NEEDS_ACTION` 和公开原因，内部备注只在事务所端可见 |
| F6-A2 | 客户完成两轮提交后切换审核页轮次 | 全页资料数量、文件、客户备注和审核结论同步切换；默认最新轮，历史轮不能触发审核或整单操作 |
| F6-A3 | 当前轮提交多份文件后保存审核结论 | 无需逐份勾选，当前资料项的可用文件自动记录到本次审核决定中 |
| F6-A4 | 尚有必填项未满足时点击批准 | 按钮不可用或后端拒绝；页面指出未满足项，状态不变 |
| F6-A5 | 全部满足后批准，再由管理员撤回 | 进入 READY_FOR_BOOKKEEPING；撤回需填写原因并返回 IN_REVIEW，时间线完整 |
| F6-A6 | 两名会计同时审核同一项 | 后提交者看到 409 冲突和最新状态，未提交备注可复制/恢复，不覆盖他人结果 |
| F6-A7 | AI 返回建议、超时和非法响应 | 合法建议只作提示；超时/非法响应有独立错误，不阻塞人工审核 |
| F6-A8 | 通知服务不可用 | 页面业务操作照常成功，通知失败不被显示成审批失败 |
| F6-A9 | 完整执行“创建发布 → 上传 → 退回 → 补交 → 满足 → 批准” | 前后端真实环境闭环成功，Playwright 关键路径通过 |
| F6-A10 | 使用 CLIENT_SUBMITTER 检查同一请求 | 只看见公开意见和允许动作，看不到内部备注、AI 原文和管理按钮 |
| F6-A11 | 用键盘操作审核动作和确认弹窗，并在窄屏查看 | 焦点不会丢失或困住，错误可读；窄屏仍能完成审核 |
| F6-A12 | 客户收到补交要求后上传新文件，并移除上一轮错误文件 | 新文件追加在原文件之后；移除仅影响本轮有效清单，上一轮提交和审核记录仍可追溯 |

### 9.4 阶段完成标志

前端覆盖账户、会计工作台和客户门户的首版完整流程。暂不加入流程画布、看板、自定义视图编辑器、离线模式和前端生成式 AI；出现明确业务需求后再评估。

### 9.5 本地验收候选（2026-09-22）

- 已加入三栏审核工作台、公开说明与内部备注；活动记录统一由请求详情页展示；
- 已接入补交、批准和管理员撤回批准动作，客户门户可继续新一轮上传；
- 前端自动检查：ESLint 通过，Vitest `46 passed`，生产构建通过。

F5/F6 中的文件名占位分类、“AI 建议只采纳到人工表单”和“当前轮文件自动全选为证据”是已验收基线；F7 将分别替换为真实 Agent 分类、可自动执行的单项审核和显式 evidence 选择，不改写历史验收记录。

## 10. F7：AI 快速分类与提交后审核

### 10.1 阶段目标

接入 B6 与 `acc-system-agent`：上传阶段只进行完整文件的快速分类，客户提交后才显示完整审核结果。AI 可按每个请求的阈值自动满足或退回单项，会计可覆盖结果并保留最终整单批准权。

### 10.2 F7.1：请求 AI 策略

- 创建请求增加 `OFF | SUGGEST | AUTO_REVIEW` 模式；界面以“确认资料符合要求 / 要求客户补交”分别选择文字档位：更多自动处理（内部 `0.950`）、平衡处理（推荐，内部 `0.980`）、优先人工复核（内部 `0.995`），附处理偏好说明。创建、编辑、确认和详情页均不直接显示数值；前端字段 `aiSatisfyThreshold`、`aiRequestActionThreshold` 仍经 Axios 转为后端 snake_case，后端范围保持 `0.500–1.000`。历史非预设值显示“保留原有自定义设置”，编辑/复制时不自动换档；
- 资料要求只填写资料类型、说明和是否必交；不展示内部 `analysis_type`，不要求手填交易日期、描述、金额或币种；
- Backend 按资料类型设置内部分析提示，提交后的 REVIEW 根据完整资料确定检查内容。银行交易从客户提交的对账单中提取，会计只在审核时处理异常或修正提取结果；完整分析只在提交后执行，模型未接入时明确展示模拟/人工处理提示，不在创建页伪装已完成；
- 策略只能在 `DRAFT` 修改，复制请求时复制策略，最后确认步骤展示完整摘要。

### 10.3 F7.2：上传快速分类

批量上传 Dialog 保持固定顺序：

```text
确认文件 → 上传/安全扫描 → AI 分类加载 → 按类别展示 → 拖动调整 → 确认合入
```

- 分类读取完整 PDF/图片，但页面只展示文件图标/名称、建议类别、目标资料项和置信度；
- 结果区包含 requirement 分组、`OTHER` 和 `INVALID`，不为每个文件再放 Select；
- 上传分类不展示主体、期间、金额、发票号、审核意见或补交建议；
- 未确认文件不进入正式清单；取消前二次确认，确认取消后清单不变；
- Agent 失败时显示明确错误和“手动分类”入口，不阻塞客户提交；
- 单项选择/拖动上传继续保留，使用和批量分类相同的文件卡片。

### 10.4 F7.3：提交后完整审核

- 客户点击提交后页面进入只读并显示审核处理状态；提交前不展示完整审核结果；
- 会计审核页展示 REVIEW run 状态、模型版本、重试、字段提取、主体/期间检查、当前/历史 evidence、结构化金额关系和转人工原因；
- AI 自动决定和人工决定视觉分离，自动决定标识为“AI 自动审核”；
- 会计显式选择 evidence 及其关系，可追加人工决定覆盖 AI 当前结果；
- 低置信度、计算不一致、非法证据或 Agent 失败显示为待人工处理，不伪装成审核失败；
- 即使全部单项被 AI 标记满足，“批准进入记账”仍只能由会计点击；
- portal 只显示公开客户说明和状态，不返回 AI 原始输出、置信度、内部备注或搜索轨迹。

### 10.5 可验收场景

| ID | 场景与操作 | 预期结果 |
| --- | --- | --- |
| F7-A1 | 创建请求时设置 AI 模式与双阈值，再复制请求 | 确认页显示正确策略；复制值一致；非 DRAFT 不显示编辑入口 |
| F7-A2 | 批量上传 requirement、OTHER 和 INVALID 文件 | 先完成上传/扫描，再按类别展示；页面无主体、期间、金额或审核结论 |
| F7-A3 | 拖动调整后确认，另一次上传中确认取消 | 首次按人工分类合入；第二次正式清单不变 |
| F7-A4 | Agent 分类失败 | 可切换手动分类并继续提交 |
| F7-A5 | 客户提交后 Agent 审核 G02/G03 | 客户看到公开补交要求；会计看到 AI 来源、evidence 和检查结果 |
| F7-A6 | 审核 B01/B03/F04/F05 | 界面展示多文件证据、历史资料和结构化金额关系 |
| F7-A7 | finding 低置信度或 Agent 超时 | 显示待人工处理与重试，人工表单仍可使用 |
| F7-A8 | AI 自动决定后会计选择 evidence 并提交新决定 | 当前结果更新，AI 决定仍在历史中可追溯 |
| F7-A9 | AI 将全部必填项标记满足 | 整单仍为 IN_REVIEW，只有会计可点击批准 |
| F7-A10 | 使用客户账户检查网络响应 | 上传分类仅返回最小类别/目标/分类置信度；不包含审核 AI 原文、审核置信度、内部备注、storage key 或搜索轨迹 |

### 10.6 阶段完成标志

真实 Agent 完成上传快速分类和提交后完整审核；自动单项决定、人工覆盖和人工整单批准形成可回归闭环。Agent 关闭时人工流程仍可完成。

### 10.7 首个可视化验收批次：F7.1 + F7.2（2026-09-22，用户验收通过）

本批连同 B6.2/A2 交付 **请求配置 + 上传分类确认**；B6.1/A1 属于内部技术检查，不单独要求用户验收接口。F7.3 的提交后完整审核、自动决定未启用，不视为完成。

入口：`http://localhost:5173`（Vite 代理本地 Docker 后端）。已有账号不改密码；另提供仅本地的客户验收账号 `ai@test.com / ai123`，对应 **F3 Acceptance Demo / January 2027**，尚未提交审核。不能将此短密码用于生产。

| 用户操作 | 应看到的结果 |
| --- | --- |
| 会计新建请求 | AI 模式默认自动单项审核，两项处理偏好均为“平衡处理（推荐）”；可选择更偏向自动或人工的档位；当前仅保存审核策略的提示明确 |
| 新增银行对账单资料项 | 只需填写业务资料要求，不出现“分析类型”和交易录入表单；空交易数据可正常保存；复制不会携带原请求的单笔交易目标，原请求历史数据不删除 |
| 客户选择多个 PDF/图片 | 先在 Dialog 核对文件；点击确认后依次上传、扫描、分类；确认类别前正式清单不增加文件 |
| 查看并调整类别 | 按资料项、其他支持文件、无效文件分组；卡片带文件图标，可拖动，也可点选文件后“移到这里”；没有逐文件 Select |
| 确认合入 | 有效文件进入对应清单，无效文件不加入；只保存一次，不再上传第二次 |
| 中途取消 | 先询问是否取消；继续处理保留选择，确认取消保持清单不变 |
| 分类失败或等待过久 | 自动进入手动归类，或扫描后点击“改为手动分类”；无需重新上传；扫描失败文件不能合入 |

- 当前使用明确标注的 **模拟分类**，只为验收流程；没有真实模型，不能验收分类准确率。沿用 shadcn/Reka Dialog、Select、DatePicker 和现有文件卡片，不另建 UI 框架。
- 旧的仅传文件名 `/classify` 已被 staged classification run 替代；单项上传保持兼容。
- 提交前不做主体/期间/金额审核；页面不会因此提前出现审核结论。
- 自动检查与浏览器检查详情见后端和 Agent 同批记录；未提交代码或部署线上。

### 10.8 F7.3 第一批：提交后分析建议（2026-09-23，用户验收通过）

- 上传阶段保持分类；客户提交后自动启动 REVIEW，客户端仅显示“正在检查 / 等待会计审核”，资料仍只读。
- 会计审核沿用整体轮次选择；对应轮次的 AI 分析随资料项切换。页面只展示运行状态、主体/期间检查、建议客户说明、当前/历史证据、提取字段和结构化金额关系，不显示模型版本、模拟标签、搜索计数或阶段性操作说明。
- 证据点击复用现有文件预览 Dialog。错误和低置信度不禁用人工审核；失败时可重试，重试保留旧 run。
- 本批 `SUGGEST` 和 `AUTO_REVIEW` 均只生成建议，`OFF` 不启动分析。自动单项决定、显式人工 evidence 选择/覆盖和 Outbox 留到 B6.4/F7.3 第二批；不将本批标记为完整 F7 完成。
- 本地案例：`AI Review Demo / March 2027`，会计 `review@test.com / review123`，客户沿用 `ai@test.com / ai123`。仅用于本地，不部署这些短密码到生产，不更改已有账号密码。

| 页面验收操作 | 预期结果 |
| --- | --- |
| 进入模拟案例，选 G02/G03 | 分别显示期间/主体不匹配、补交建议；资料状态仍为已收到，不自动退回 |
| 选 B01、F02、F04、F05 | 展示多文件证据、合计/减法/外币转换与手续费金额关系；区分后端算术重算与文件真实性核实 |
| 选 B03 并点击历史证据 | 显示搜索得到的历史文件并可预览，不将文件复制到本轮上传清单 |
| 展开提取字段 | 显示主体、期间、单据编号、对手方、金额和银行交易；缺失信息显示尚未验证 |
| 提交普通文件 | 模拟模型不伪造识别能力；转人工建议，人工决定表单继续可用 |
| 切换提交轮次 | 只显示对应轮次的分析结果；新轮次不会覆盖旧 run 输出 |
| 查看客户门户 | 仅公开处理状态和人工已保存的客户说明，不显示模型输出、置信度和搜索细节 |

页面文案沿用英/中 i18n，组件沿用既有 app-panel、Button 和预览 Dialog。自动测试及本地链路记录见后端同批说明。

浏览器实测：会计登录、G02 期间不匹配展示、切换 B03 历史证据及历史 PDF 的 Dialog 预览通过。旧验收标签页一度不响应自动点击，重开标签页后正常；未因此改变现有选择控件或记录伪造的人工审核结论。

用户验收后补充：AI 明确建议可一键填入右侧审核表单但不自动保存；整单处于审核中时单项结论可重新修改；任一资料项未保存结论时禁止退回或批准；旧数据中已退回但尚未审核的资料可继续编辑并自动带入新轮次。审批操作条固定在视口底部，豁免弹窗仅作二次确认。前端自动检查 **49 passed**，lint 和构建通过。

## 11. 阶段验收记录模板

每完成一个阶段，在 issue/release 中保存：

```text
阶段：F1/F2/...
frontend 提交 SHA：
backend 提交 SHA：
frontend 镜像：
backend API 版本/镜像：
验收环境与浏览器：
自动测试结果：
手工场景结果：
未完成项：
验收人：
日期：
```
